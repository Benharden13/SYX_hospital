首末次规则：

1.首末次均有默认值，可修改
2.首末次均不能大于频次的最大执行次数
3.当天开当天停的医嘱（停止日期等于开始日期），以末次为准，并且末次不能大于首次

首末次的默认值规则如下：

（1）长期医嘱首次默认值：根据开始时间和频次的执行时刻 算出频次的剩余执行次数，则首次的默认值是（频次的剩余执行次数+1）
     举例：频次 tid(12p,4p,8p)  ,开始时间是5p，那么该频次的剩余执行次数是1次（8p这个时刻），那么首次的默认值是2
（2）临时医嘱无首次默认值，但是可以录入首次，不能录入0，不能大于频次的最大执行次数
（3）长期医嘱末次默认值：停止医嘱时，末次默认频次的最大执行次数，当天开当天停，末次默认为首次
（4）临时医嘱无末次概念，不可以编辑

